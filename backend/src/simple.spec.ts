// Need to keep it until we have a proper tests, because coveralls crashes if we have no tests. After that please remove this file.
describe("An example test", () => {
  it("should return 4", () => {
    expect(2 + 2).toBe(4)
  })
})
